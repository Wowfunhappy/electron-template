{
  "name": "Electron Wrapper",
  "version": "2024.08.25",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "package-mac": "NODE_TLS_REJECT_UNAUTHORIZED=0 electron-packager . --platform=darwin --arch=x64 --out=release --overwrite"
  },
  "devDependencies": {
    "@electron/packager": "^18",
    "electron": "3.1.13"
  }
}
